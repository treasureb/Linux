#include<stdio.h>
#include<string.h>

int main()
{
    const char *msg1 = "hello,fwrite!\n";
    const char *msg2 = "hellp print"
    fwrite(msg,strlen(msg),1,stdout);

    printf("hello,printf!\n");
    fprintf(stdout,"hello,fprintf!\n");
    return 0;
}
